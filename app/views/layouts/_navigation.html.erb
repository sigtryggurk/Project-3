<%= link_to "SiggiStickies", root_path, :class => 'brand' %>
<ul class="nav">
  <% if current_user %>
    <p>Logged in as <b><%= current_user.name %></b> (<%= current_user.email%>) <a id="log-out" >Log out</a></p>
  <% else %>
    <li>
    <%= link_to "Sign in", new_sessions_path %>
    </li>
    <li>
    <%= link_to "Register", new_users_path %>
    </li>
  <%end%>
</ul>

